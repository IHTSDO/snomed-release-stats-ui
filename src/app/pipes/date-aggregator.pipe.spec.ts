import { DateAggregatorPipe } from './date-aggregator.pipe';

describe('DateAggregatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAggregatorPipe();
    expect(pipe).toBeTruthy();
  });
});
