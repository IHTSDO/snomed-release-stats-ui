import { DateOrderPipe } from './date-order.pipe';

describe('DateOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
