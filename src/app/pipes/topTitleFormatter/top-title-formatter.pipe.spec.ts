import { TopTitleFormatterPipe } from './top-title-formatter.pipe';

describe('TopTitleFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopTitleFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
